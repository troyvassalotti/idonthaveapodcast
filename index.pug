doctype html
html(lang="en" dir="ltr")
    head
        - var url = "https://www.idonthaveapodcast.com"
        - var title = "I Don't Have A Podcast"
        - var description = "Are all your friends starting podcasts? Does that make you never want to start your own? Proclaim your dedication to never start your own podcast."

        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title=title

        link(rel="stylesheet" href="https://unpkg.com/@picocss/pico@latest/css/pico.classless.min.css")

        link(rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🎙️</text></svg>")
        meta(name="description" content=description)
        meta(name="og:locale" content="en_US")
        meta(name="og:type" content="website")
        meta(name="og:title" content=title)
        meta(name="og:description" content=description)
        meta(name="og:site_name" content=url)
        meta(name="twitter:card" content="summary")
        meta(name="twitter:title" content=title)
        meta(name="twitter:description" content=description)
        link(rel="canonical" href=url)

    body
        main
            h1=title
            p=description
            h2 Why Does This Exist
            p When was the last time one of your friends told you they were starting a podcast? Yesterday? Yeah, that sounds about right. We all love podcasts as much as the next person, but we can also agree that the number of podcasts available to us right now is messed up.
            blockquote Friends don't let friends contribute to the overloaded podcast landscape.
            p You may #[em want] to start your own podcast... But don't. Hold out. Restrain yourself from creating a show based around pairing the perfect Yankee Candle with any given brunch order at all the local spots in your city.
            p ...
            p Okay, real talk? That'd be a pretty dope podcast. BUT DON'T MAKE IT.
            h2 What Are You Supposed To Do?
            p Pledge your allegiance not to a flag, but to the fight against starting your own podcast. Sign this site's petition (it's a simple form below) and tell the world where you stand.
            h2 Fellow Never-Podcasters

            -
                var people = [
                    {
                        name: 'Troy V',
                        message: "Find me on Twitter @validcharacters"
                    },
                    {
                        name: 'Lyndsi J'
                    },
                    {
                        name: 'Kevin H',
                        message: "I don’t even remember the last time I listened to a podcast.",
                        note: "Watch out for this Badass."
                    }
                ]

            ul
                each person in people
                    if person.message
                        if person.note
                            li #{person.name}: #{person.message} #[strong Editor's Note:] #{person.note}
                        else
                            li #{person.name}: #{person.message}
                    else
                        li #{person.name}
            h3 Sign Your Name To Fight Back
            form(name="Never Starting A Podcast" data-netlify="true" netlify-honeypot="bot-field")
                label(style='display: none;') Don't fill this out: #[input(name='bot-field')]
                label(for='name') Name
                input#name(name='name' type='text' required)
                label(for='message') Message
                textarea#message(name='message' placeholder='Tell me anything else I should know (email, social handles, why, etc...)' rows='5')
                input#submit(type='submit' value='Submit')
